#!/bin/bash

# === Build Configuration ===
export BUILD_CONFIG=Debug # Change to Debug, Release, or Dist depending on your preferred build configuration
export COMPILER=clang # Change to clang or gcc depending on your preferred compiler